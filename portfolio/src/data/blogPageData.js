import Her from './articles/her';
import Drive from './articles/drive';

const blogPageData = [
    {
      id: Her.id,
      pathway: Her.pathway,
      category: Her.category,
      title: Her.title.title1,
      picture: Her.picture.img1,
      description: Her.description,
    },
    {
      id: Drive.id,
      pathway: Drive.pathway,
      category: Drive.category,
      title: Drive.title.title1,
      picture: Drive.picture.img1,
      description: Drive.description,
    }
]

export default blogPageData;